{
  "name": "hostel-hub-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix fronted",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix fronted",
    "start": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}