{
  "name": "hostel-hub-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix fronted",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix fronted",
    "dev": "concurrently -k -n BE,FE -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix fronted\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
